language: node_js
node_js:
  - '6'
cache:
  - yarn
before_install:
  - cd src
  - npm install -g yarn
install:
  - yarn
script:
  - npm run test-cov-lcov
after_success:
  - 'cat ./coverage/lcov.info | ./node_modules/.bin/coveralls'
